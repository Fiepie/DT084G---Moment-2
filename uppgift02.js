"use strict";

function multiplicateNumbers(num1, num2) { //Function with name that indicates the purpose of the function.
    console.log(num1 * num2); //Print of function
}

multiplicateNumbers(55, 89);//Argument of function