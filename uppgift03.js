"use strict";

let array = ['purple', 'lightgreen', 'yellow', 'pink', 'orange', 'black', 'blue'] //Array with 7 colors. 
let colors = array.length; //variabel that checks how many colors are in index.

//loop through array
for (let i=0;  i < array.length; i++) {  
    console.log(array[i]); //Print of array of colors. 
}
console.log("Antal värden: " + colors); //Print of how many colors that are in the array.
